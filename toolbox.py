# Week 4 Assignment: Hands-On Lab — Grades, Eligibility & Smart Decisions

This assignment practices Python functions, parameters, return values, and default parameter values.

- `welcome.py` - Contains the `welcome()` function that creates personalized PLP welcome messages.
- `toolbox.py` - Contains the `double()`, `is_pass()`, and `greet()` functions and tests them with different values.

The `greet()` function was the hardest to write because it uses a default parameter while also allowing a different greeting to be provided. It helped me understand how default parameter values work in Python.